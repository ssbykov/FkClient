package ru.faserkraft.client.domain.qr

/**
 * Источник, из которого получено значение QR-кода.
 * Используется для ограничения бизнес-логики (например, создание нового продукта
 * или регистрация устройства должны запускаться только при реальном сканировании,
 * а не при ручном вводе серийного номера).
 */
enum class QrInputSource {
    CAMERA,
    MANUAL_INPUT,
}
